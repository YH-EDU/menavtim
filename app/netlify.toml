# Netlify deploy — clean URL at menavtim.netlify.app (no GitHub username)
[build]
  command = "npm run build:netlify"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
